#!/command/with-contenv bashio
# ==============================================================================
# Home Assistant Community Add-on: Network UPS Tools
# PowerMan daemon finish script
# ==============================================================================
if [[ "${1}" -ne 0 ]] && [[ "${1}" -ne 256 ]]; then
  bashio::log.warning "PowerMan daemon crashed"
fi

bashio::log.info "PowerMan daemon stopped"
